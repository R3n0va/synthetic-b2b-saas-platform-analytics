# Representative source dataset

The committed smoke-profile data contains all governed source tables at reduced volume. It is deterministic and intended for schema inspection, local verification and lightweight SQL or BI development. Full portfolio data is written to `data/generated/default/` and is excluded from Git.
